{
  "name": "cfncr-get-vpc",
  "version": "0.1.0",
  "description": "Lambda function for CloudFormation custom resource to retrieve shared VPC properties",
  "main": "dist/handler.js",
  "rootDir": "src",
  "scripts": {
    "build": "tsc",
    "postbuild": "cp package.json dist/cfncr-get-vpc"
  },
  "private": true,
  "license": "Apache-2.0",
  "dependencies": {
    "aws-sdk": "^2.1135.0"
  },
  "devDependencies": {
    "@types/node": "^17.0.35",
    "typescript": "^4.5.4"
  }
}
